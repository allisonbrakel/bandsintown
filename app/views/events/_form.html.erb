<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_date %>
    <%= form.text_field :event_date, id: :event_event_date %>
  </div>

  <div class="field">
    <%= form.label :onsale_date %>
    <%= form.text_field :onsale_date, id: :event_onsale_date %>
  </div>

  <div class="field">
    <%= form.label :band_id_id %>
    <%= form.text_field :band_id_id, id: :event_band_id_id %>
  </div>

  <div class="field">
    <%= form.label :venue_id_id %>
    <%= form.text_field :venue_id_id, id: :event_venue_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
