<%= form_with(model: band, local: true) do |form| %>
  <% if band.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(band.errors.count, "error") %> prohibited this band from being saved:</h2>

      <ul>
      <% band.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :band_name %>
  </div>

  <div class="field">
    <%= form.label :event_count %>
    <%= form.number_field :event_count, id: :band_event_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
